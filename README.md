# SomonLogistics

Flutter app for Somon Logistics (`com.somonlogistics.app`).

## Run

```bash
flutter pub get
flutter run
```

Place Firebase config files locally (`GoogleService-Info.plist`, `google-services.json`); they are not committed.
